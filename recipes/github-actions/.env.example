# Uncomment and set only the variables you need. Existing exported values can be used as-is.
# Required: CoreWeave token with SANDBOX_USER, from https://console.coreweave.com/tokens.
# CWSANDBOX_API_KEY="[COREWEAVE-API-ACCESS-TOKEN]"
# Optional: Anthropic API key for --review, from https://console.anthropic.com/settings/keys.
# ANTHROPIC_API_KEY="[ANTHROPIC-API-KEY]"
# Optional: Override this recipe's review model without changing ANTHROPIC_MODEL for other tools.
# SANDBOX_REVIEW_MODEL="claude-sonnet-5"
